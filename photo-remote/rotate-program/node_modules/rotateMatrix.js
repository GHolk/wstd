// Generated by CoffeeScript 1.12.1
(function() {
  var cos, matrix, rotateMatrix, sin;

  matrix = require('matrix');

  sin = Math.sin, cos = Math.cos;

  rotateMatrix = {
    omega: function(w) {
      return matrix.createFromArray2D([[1, 0, 0], [0, cos(w), sin(w)], [0, -sin(w), cos(w)]]);
    },
    phi: function(f) {
      return matrix.createFromArray2D([[cos(f), 0, -sin(f)], [0, 1, 0], [sin(f), 0, cos(f)]]);
    },
    kappa: function(k) {
      return matrix.createFromArray2D([[cos(k), sin(k), 0], [-sin(k), cos(k), 0], [0, 0, 1]]);
    },
    wfk: function(w, f, k) {
      return this.kappa(k).multiply(this.phi(f)).multiply(this.omega(w));
    }
  };

  module.exports = rotateMatrix;

}).call(this);
